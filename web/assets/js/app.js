/**
 * Created by SMITHE on 12-Dec-16.
 */

$( document ).ready( function () {
	/*$( '.qteProductCart' ).change( function ( e ) {
		let qteProduct = $( e.currentTarget );
		let form       = qteProduct.parents( 'form' );
		let idProduct  = form.find( 'input:hidden[name=idProduct]' );
		
		console.log( idProduct[ 0 ].value, qteProduct[ 0 ].value );
		
		$.post( Routing.generate( 'sil21_changeQuantityProduct', {
			id:  idProduct[ 0 ].value,
			qte: qteProduct[ 0 ].value
		} ) )
			.done( function ( data ) {
				console.log( data );
			} )
	} )*/
} );