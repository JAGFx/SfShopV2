{% block listCategories %}
	<h3 >{{ 'brand.entity'|transchoice(2, {}, 'AdminBundle') }}</h3 >
	<ul >
		{% for brand in brands %}
			<li ><a href="{{ path("brand_articleByBrand" ,  { 'id': brand.id } ) }}" >{{ brand.name }}</a ></li >
		{% endfor %}
	</ul >
{% endblock %}