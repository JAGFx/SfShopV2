{% extends '::layout.html.twig' %}

{% block content %}
	<h1>{{ name }} <small>{{ products|length }} {{ 'product.entity'|transchoice(products|length, {}, 'AdminBundle') }}</small></h1>
	{% for product in products %}
		{% include 'sil21VitrineBundle:Product:widget.html.twig' %}
	{% endfor %}
{% endblock %}